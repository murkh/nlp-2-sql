[project]
name = "text2sql"
version = "0.1.0"
description = "Enterprise-grade Text-to-SQL RAG Agent with multi-layer security, RBAC, and conversation history"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.30.0",
    "python-dotenv>=1.0.0",
    "chromadb>=0.5.0",
    "psycopg2-binary>=2.9.0",
    "anthropic>=0.25.0",
    "pyyaml>=6.0",
    "sqlparse>=0.5.0",
    "tiktoken>=0.7.0",
]
